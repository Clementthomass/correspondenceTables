% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classificationEndpoint.R
\name{classificationEndpoint}
\alias{classificationEndpoint}
\title{List available classification schemes from CELLAR and/or FAO}
\usage{
classificationEndpoint(endpoint = "ALL")
}
\arguments{
\item{endpoint}{Character string indicating which endpoint to query.
Valid values are:
\itemize{
  \item \code{"CELLAR"}: query only the CELLAR repository,
  \item \code{"FAO"}: query only the FAO repository,
  \item \code{"ALL"}: query both and return a list containing the results.
}}
}
\value{
If \code{endpoint = "CELLAR"} or \code{"FAO"}, a
\code{data.frame} with columns:
\code{Prefix}, \code{ConceptScheme}, \code{URI}, \code{Title}.  

If \code{endpoint = "ALL"}, a named list with elements:
\itemize{
  \item \code{$CELLAR}: data frame as above (if available),
  \item \code{$FAO}: data frame as above (if available).
}
}
\description{
This function retrieves all classification schemes available in the
CELLAR and/or FAO SPARQL endpoints.  
It provides, for each scheme, the structural information needed to build
subsequent SPARQL queries, such as retrieving the full classification
table or its hierarchy levels.

The returned metadata includes:
\itemize{
  \item \strong{Prefix}: namespace prefix used in SPARQL queries,
  \item \strong{ConceptScheme}: the internal concept-scheme identifier,
  \item \strong{URI}: the base URI of the classification scheme,
  \item \strong{Title}: human-readable English label of the scheme.
}
}
\details{
The function performs HTTP POST SPARQL requests to:
\itemize{
  \item the CELLAR endpoint  
        \url{http://publications.europa.eu/webapi/rdf/sparql},
  \item the FAO endpoint  
        \url{https://caliper.integratedmodelling.org/caliper/yasgui/sparql}  
        (can be overridden via the global option  
        \code{options(correspondenceTables.fao_endpoint = "...")}).
}

In case of network errors or invalid responses:
\itemize{
  \item for \code{endpoint = "ALL"}, warnings are issued and only the
        reachable endpoints are returned;
  \item for \code{endpoint = "CELLAR"} or \code{"FAO"}, a clear error is
        thrown informing the user that the endpoint is unavailable.
}
}
\examples{
\dontrun{
  # Retrieve all schemes from both endpoints
  x <- classificationEndpoint("ALL")

  # Retrieve only CELLAR schemes
  cellar <- classificationEndpoint("CELLAR")

  # Retrieve only FAO schemes (custom endpoint allowed)
  options(correspondenceTables.fao_endpoint =
    "https://caliper.integratedmodelling.org/caliper/yasgui/sparql")
  fao <- classificationEndpoint("FAO")
}

}
\seealso{
\code{\link{dataStructure}}  
\code{\link{retrieveClassificationTable}}
}
